#!/usr/bin/env node

/**
 * 현재 미사용(OFF): 노션 릴리즈 싱크 비활성화 상태.
 * 재활성화 시 워크플로를 `.github/workflows`로 복구한 뒤 이 스크립트를 연결해 사용한다.
 */

console.log("[OFF] notion release sync is currently disabled.");
process.exit(0);
